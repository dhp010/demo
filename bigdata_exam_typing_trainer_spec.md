# 손코딩 트레이너 — 재현용 스펙 문서

이 문서는 `bigdata_exam_typing_trainer.html`을 처음부터 다시 만들 수 있도록,
지금까지 만들어진 결과물의 목적·구조·동작·디자인·데이터 규칙을 정리한 것이다.
다른 AI가 이 문서만 보고 동일한 결과물을 재구현할 수 있는 것을 목표로 한다.

## 1. 목적

빅데이터분석기사 실기(파이썬) 시험 합격을 위한 **연습용 타이핑 트레이너**.
사용자가 "이런 상황에서 어떤 코드를 쓰는가"라는 설명(한글)을 보고, 그에 해당하는
정답 파이썬 코드를 직접 타이핑해서 외우는 것이 목적이다. 실제 코딩 실행/채점은
하지 않고, 문자열 비교로만 정답 여부를 판단하는 **정적 단일 HTML 파일** 앱이다.

## 2. 기술 스택 / 제약

- 순수 HTML + CSS + Vanilla JS. 프레임워크, 빌드 도구, 외부 JS 라이브러리 없음.
- 파일 하나로 완결(`<style>` + `<script>` 인라인). 서버/백엔드 없음, 브라우저에서 파일 열면 바로 동작.
- 진행상황은 `localStorage`에 영속화된다(§6.1). 새로고침해도 완료·오답노트·입력 내용이 유지된다.
- 구글 폰트 2종만 외부 리소스로 사용: `IBM Plex Sans KR`(UI), `JetBrains Mono`(코드).
- IIFE로 전체 스크립트를 감싸 전역 오염 방지.

## 3. 비주얼 디자인 시스템

밝고(light) 화사한 톤. 모든 색상은 CSS 커스텀 프로퍼티(`:root`)로 정의하고,
스타일 어디서도 하드코딩된 색 대신 변수만 참조한다(에디터 탭바의 신호등 점 3개 제외 — 장식용 고정색).

```css
:root{
  --bg:#f5f7fb;            /* 페이지 배경 */
  --surface:#ffffff;       /* 카드/패널 배경 (bg보다 밝음) */
  --surface-2:#eef1f7;
  --surface-3:#e3e7f0;
  --border:#dde2ec;
  --border-strong:#c7cee0;
  --text:#1e2433;          /* 본문 텍스트 (짙은 네이비) */
  --text-dim:#5f6779;
  --text-faint:#9aa2b5;
  --accent:#e08b2e;        /* 포인트(주황) */
  --accent-dim:#b8691c;
  --accent-soft:rgba(224,139,46,0.14);
  --correct:#15915b;       /* 정답/성공(초록) */
  --correct-soft:rgba(21,145,91,0.14);
  --wrong:#e0355f;         /* 오답/에러(빨강) */
  --wrong-soft:rgba(224,53,95,0.13);
  --radius:10px;
  --font-ui:'IBM Plex Sans KR','Malgun Gothic',sans-serif;
  --font-code:'JetBrains Mono','Consolas',monospace;
}
```

원칙: `surface`는 항상 `bg`보다 밝게(대비되게) 잡아서 카드가 배경 위에 떠 보이도록 한다.
포인트/정답/오답 색은 흰 배경에서도 텍스트로 쓸 때 충분한 대비가 나오도록 일반적인
브랜드 색보다 한 톤 어둡게(`accent-dim`, 진한 green/red) 잡는다.

## 4. 레이아웃 / 화면 구조

```
<header> (sticky top)
  - 브랜드(타이틀 + 부제)
  - 전체 진행률 바 (n/총 완료, progress-fill)
  - "★ 오답노트" 토글 버튼(칩 형태)
  - "↺ 전체 초기화" 버튼 (localStorage 삭제 + 전체 상태 리셋, confirm 후 실행)

<nav class="tabs">  4개 실전 탭 + "전체 함수 사전" 탭

<div class="workbench">  (2열 grid: 340px 사이드바 + 나머지 practice, 900px 이하에서 1열로)
  <aside class="sidebar">
    - 일반 탭: 카테고리별 <details><summary> 접이식 목록.
      summary에 카테고리명 + "완료/전체" 배지.
      각 항목은 상태 dot(미시작/진행중/완료) + 설명 텍스트 버튼.
    - "전체 함수 사전" 탭: 검색 input + 전체 항목 평면 리스트(문자열 포함검색:
      설명 또는 코드 텍스트에 매치).
  <section class="practice">
    - 아무것도 선택 안 했을 때: 빈 상태 placeholder({ } 글리프 + 안내문구).
    - 항목 선택 시 renderPracticeFor()가 아래 구조를 채움:
      1. practice-head: 브레드크럼(탭/카테고리) + 제목 + 정확도%/시간/완료뱃지
      2. (작업형1·3 및 모의고사의 작업형1/3 항목만) print-notice 경고 배너
      3. info-panel: 왜·언제 / 수식 / 예시
      4. (모의고사 항목만) problem-brief: 시나리오 설명문 + 샘플 테이블 + 단계별 지시사항(ol)
      5. editor-block "내_입력.py": 탭바(파일명 + 제출 버튼 + 다시시도 버튼) + textarea
         + feedback-panel(기본 hidden, 제출 후 오답이면 표시)
      6. hint 문구(단축키 안내 포함)
      7. editor-block "정답.py": 탭바(신호등 점 3개 + 파일명 + 정답 보기/숨기기 토글)
         + 코드 참조 뷰(line-number + 코드, 읽기 전용)
```

정답 노출 기본값: 사전 탭(t1~t3) 항목은 펼침, **모의고사(steps 보유) 항목은 접힘**.
실전처럼 먼저 스스로 작성하게 하려는 의도이며, "정답 보기" 버튼으로 언제든 펼칠 수 있다.

키보드: <kbd>Tab</kbd> 4칸 들여쓰기 / <kbd>Ctrl+Enter</kbd> 제출 / <kbd>Alt+↑↓</kbd> 항목 이동
(Alt 이동은 현재 사이드바에 **보이는** 항목만 순회하므로 오답노트 필터 상태를 그대로 따른다).

레이아웃 순서의 핵심 의도: **정답 참조 코드는 화면 맨 아래**, 사용자가 직접 타이핑하는
입력창은 그 위(문제 설명 바로 아래)에 둔다 — 답을 먼저 보지 않고 스스로 작성해본 뒤
아래로 스크롤해서 확인하는 흐름을 유도하기 위함.

## 5. 핵심 상호작용 규칙 (중요 — 시행착오 끝에 정착된 규칙)

### 5.1 실시간 채점을 하지 않는다
- 타이핑 중에는 **어떤 정답/오답 판정도 하지 않는다.** 업데이트되는 것은 정확도%(누적
  글자 단위 일치율)와 경과 시간(타이머)뿐이며, 이 두 값은 가벼운 계산이라 성능에 영향이 없다.
- 완료 판정은 텍스트 입력 이벤트에서 자동으로 하지 않는다. **"제출" 버튼을 눌러야만**
  현재 입력값과 정답을 비교한다.
- 제출 결과:
  - 완전히 일치 → 완료 처리(입력창 readonly, "완료" 뱃지 표시, 제출 버튼 비활성화,
    카테고리 배지/전체 진행률 갱신).
  - 불일치 → feedback-panel을 보여주고, **사용자가 실제로 입력한 문자열**을 한 글자씩
    `<span>`으로 렌더링해서 정답과 같은 위치는 초록(`ok`), 다르면 빨강(`bad`), 정답 길이를
    넘어선 초과 입력은 빨강+밑줄(`extra`)로 표시한다. 입력이 정답보다 짧으면 "정답보다
    O자 짧습니다" 안내문도 덧붙인다. 이 시점에 해당 문제는 "오답노트" 대상으로 영구
    플래그(`everWrong`)된다(이후 맞혀도 플래그는 유지 — 복습 대상 추적용 의도된 동작).
  - 다시 타이핑을 시작하면 feedback-panel은 즉시 숨김 처리(재제출 전까지 판정 무효).

### 5.2 왜 이렇게 되었는가 (다른 AI가 같은 실수를 반복하지 않도록)
1. **처음에는** 타이핑 중 실시간으로 정답 텍스트를 "고스트 텍스트"(연한 색으로 남은
   글자를 미리 보여줌)로 겹쳐 보여주는 방식이었다 → 사용자가 "타이핑 공간에 답이
   보이면 안 된다"고 요청 → 안 보이게 로직만 수정(비교는 여전히 매 키 입력마다 실행).
2. 그 다음 "타이핑 속도를 못 따라가고, 맞게 쳤는데도 틀렸다고 뜨는 곳이 많다"는
   버그 리포트 발생. 원인 조사 결과, **키 입력마다 다음이 반복 실행되고 있었다**:
   - 전체 문제 수백 개를 훑는 카테고리 완료 배지 재계산(사실 완료 시점에만 바뀌는 값인데
     매 키 입력마다 다시 계산 — 완전히 불필요한 중복 작업).
   - 사이드바의 모든 버튼을 `querySelectorAll`로 다시 조회해서 hidden 클래스를
     토글하는 오답노트 필터 재적용(이 값도 "wrongOnly 필터가 켜져 있고, 방금 이 문제가
     처음 오답으로 바뀐 순간"에만 의미가 있는데 매번 실행됨).
   - 타이핑한 글자 수만큼 `<span>`을 통째로 새로 만들어 덮어씌우는 글자 단위 오버레이
     렌더링(사용자가 타이핑할 때마다 O(n) DOM 재생성, 여기에 다음 항목의 O(n^2) 누적).
   위 낭비 요소들을 제거(불필요한 호출 삭제, 상태 전이 시점에만 실행되도록 가드)했지만
   그래도 지연이 남아 있었다 → 사용자가 "아예 실시간 채점 자체를 없애고 자유롭게
   타이핑하게 해달라"고 제안 → **글자 단위 실시간 오버레이/채점을 완전히 제거**하고
   평범한 `<textarea>`로 전환(네이티브 입력이라 렉이 원천적으로 없음).
3. 이후 "제출 버튼을 눌렀을 때만 비교하고, 틀린 곳을 표시해달라"는 요청으로 지금의
   **제출 트리거 방식 + 결과 패널(피드백 박스)**로 정착됨. 이 방식은 무거운 글자별
   diff 렌더링을 "버튼 클릭 시 1회"로만 실행하므로 실시간 성능 문제와 실시간 채점
   방식의 UX 문제(캐스케이드 오탐 등)를 동시에 해결한다.

**결론: 글자 단위 정오 비교/렌더링은 절대 keyup/input 이벤트에 걸지 말고, 명시적인
"제출" 액션에서만 1회 실행할 것.** 진행률/정확도%/타이머처럼 O(1)~O(n) 수준의 가벼운
연산만 입력 이벤트에 걸어도 된다.

### 5.3 정답 비교 방식
- 비교 전 양쪽 모두 `normalizeForCompare()`로 정규화한다:
  1. `\r\n` → `\n`
  2. **각 줄의 끝 공백·탭 제거**
  3. **마지막 빈 줄 제거**
  그 외의 차이(들여쓰기, 괄호, 콤마, 따옴표, 줄 사이 공백)는 전부 유의미하게 취급한다.
- 이 정규화가 없으면 답을 다 치고 습관적으로 Enter를 한 번 더 누른 것만으로 오답 처리되고,
  오답노트에 영구 플래그까지 남는다. 학습 의욕을 꺾는 대표적 오탐이므로 반드시 유지할 것.
- 완료 판정: `normalizeForCompare(typed) === normalizeForCompare(target)`.
- 오답 표시(diff): 정규화된 문자열끼리 인덱스 기반 단순 비교. 정답은 보여주지 않고
  **사용자가 실제로 입력한 글자**를 색칠해서 보여준다.
- Tab 키는 4칸 스페이스로 치환해서 삽입(`preventDefault` 후 직접 textarea.value 조작 +
  수동 `input` 이벤트 dispatch).

## 6. 상태 모델

```js
state[id] = {
  typed: '',        // 현재 입력값(정규화된 \n 기준)
  completed: false, // 제출 성공 여부
  everWrong: false, // 한 번이라도 오답 제출한 적 있는지(영구, 오답노트용)
  elapsedSec: 0,    // 누적 경과 시간
  running: false,   // 타이머 동작 중인지
  startTs: null,    // 타이머 시작 시각(Date.now())
};
```
- 항목 id 형식: `{tabKey}-{categoryIndex}-{itemIndex}` (예: `t3-8-1`).
- `ITEM_INDEX[id]`에는 표시용 메타(desc, code, tabLabel, catName, tabIdx, scenario, table,
  steps, formula, example, usage)를 미리 평탄화해서 저장 — 렌더링 시 매번 DATA를 순회하지
  않도록.

### 6.1 영속화 (localStorage)

```js
localStorage['bigdata-typing-trainer/v1'] = JSON.stringify({
  v: 1,
  wrongOnly: false,
  items: { '<시그니처>': { t:'입력값', c:완료여부, w:오답여부, e:경과초 } }
})
```

- **저장 키는 위치 id(`t1-3-2`)가 아니라 내용 시그니처(`{tabKey}|{설명문}`)다.**
  위치 id로 저장하면 문제를 하나만 추가·재배치해도 저장된 진행상황이 엉뚱한 항목에
  붙는다. 시그니처 방식이면 문제를 계속 늘려도 기존 진행상황이 그대로 살아남는다.
  같은 탭 안에서 설명문이 겹치면 `|{itemIndex}`를 덧붙여 구분한다(`ID_BY_SIG`/`SIG_BY_ID`).
- 손대지 않은 항목(입력·완료·오답·시간이 모두 비어 있음)은 저장하지 않아 용량을 아낀다.
- 저장 시점: 입력(400ms 디바운스), 오답 제출, 완료, 다시 시도, 오답노트 토글, `beforeunload`.
- 복원 시 `running`/`startTs`는 항상 초기화한다(브라우저를 닫아둔 시간이 누적되면 안 됨).
- `localStorage` 접근은 전부 try/catch로 감싼다 — 시크릿 모드나 사이트 데이터 차단
  환경에서 접근 자체가 예외를 던지며, 이 경우에도 앱은 저장만 포기하고 정상 동작해야 한다.
- 저장된 데이터가 있어도 없는 항목(`ID_BY_SIG`에 매칭 실패)은 조용히 무시한다.

## 7. 사이드바/진행률 표시 규칙

- 항목 상태 dot: 기본(회색) → 입력 시작하면 진행중(포인트색) → 제출 성공 시 완료(초록).
  `everWrong`이 true면 dot 우측 상단에 작은 빨간 점(flagged) 오버레이.
- 카테고리 배지: "완료수/전체수". 전부 완료되면 강조 스타일(초록 테두리/글자).
- 헤더의 전체 진행률 바: 전체 문제 수 대비 완료 수(%) 표시.
- "★ 오답노트" 토글: 켜면 `everWrong===true`인 항목만 사이드바에 남기고 나머지는 숨김
  (해당 카테고리에 보이는 항목이 0개면 카테고리 자체도 숨김).
- 이 필터 재계산은 "필터가 켜져 있고, 방금 오답 플래그가 새로 세워진 순간"에만 실행
  (§5.2의 성능 교훈 반영).

## 8. 데이터 스키마 & 저작 규칙

```js
DATA = [
  {
    key: 't1', label: '작업형1',
    categories: [
      {
        name: '카테고리명',
        items: [
          { d: '설명(한글)', c: '정답 파이썬 코드(문자열, \\n으로 여러 줄)' },
          // 모의고사 탭 항목은 아래 필드가 추가됨:
          // scenario: '문제 상황 설명',
          // table: { cols:[...], rows:[[...], ...] },  // 샘플 데이터 표
          // steps: ['지시사항1', '지시사항2', ...],
        ]
      },
      ...
    ]
  },
  ...
]
```

4개 탭의 성격:
- **t1 작업형1**: pandas 데이터 전처리 "함수 사전" — 짧은 1~수줄 스니펫. `df`, `pd`는
  이미 import되어 있다고 가정(코드에 절대 다시 import하지 않음 — 실제 시험 환경의
  전제와 동일).
- **t2 작업형2**: sklearn 모델링 스니펫(분할/모델/평가지표/교차검증/하이퍼파라미터/제출).
- **t3 작업형3**: scipy/statsmodels 통계 스니펫(정규성/t검정/카이제곱/분산분석/상관/회귀/
  다중공선성).
- **t4 모의고사**: 회차별로 작업형1(단답 3문제) / 작업형2(모델링 1문제) / 작업형3(통계
  2문제)로 구성된 완결형 문제. 각 문제는 `scenario` + `table`(샘플 데이터) + `steps`
  (풀이 절차) + `c`(전체 정답 스크립트)를 갖는다.

### 8.1 import 규칙 (버그로 여러 번 지적받은 부분 — 반드시 지킬 것)
1. **pandas(`pd`)만 전역적으로 이미 import되어 있다고 가정**한다. 그 외 모든 외부
   라이브러리(numpy, scipy, sklearn, statsmodels, xgboost 등)는 **그 항목의 코드
   문자열 안에서 처음 쓰이기 전에 반드시 자체적으로 import**해야 한다.
   - 예외적으로 "사전" 탭(t1/t2/t3)에서 같은 카테고리 안의 바로 다음 항목이 방금 만든
     변수(`model`, `X_train`, `Q1`, `group1` 등)를 이어서 쓰는 것은 허용된다(연속 스니펫
     학습 의도). 하지만 **import는 절대 이어받지 않는다** — 매 항목이 독립적으로
     검색/열람될 수 있으므로, 함수/클래스 import는 그 항목 코드 안에 항상 다시 넣는다.
   - t4 모의고사 항목은 애초에 서로 독립된 완결 문제이므로 당연히 자체 import 필요.
2. **import 문은 항상 스크립트 맨 위에 몰아서 쓴다.** 데이터 전처리 코드 중간이나
   끝에 뒤늦게 import를 끼워 넣지 않는다(실행은 되지만 스타일이 지저분하다고 지적받음).
   여러 개면 위에서부터 순서대로 나열.
3. 이 두 규칙을 어긴 항목이 실제로 9곳 이상 발견되어 나중에 다시 고쳤다. 새로 항목을
   추가할 때 아래 체크를 코드로 자동 검증할 수 있다(참고용 의사코드):
   ```js
   // 1) 각 코드 문자열에서 "라이브러리명.함수(" 패턴이 등장하는데
   //    같은 문자열 안에 해당 import 라인이 없으면 → 오류
   // 2) 코드 문자열을 줄 단위로 순회하다가, import가 아닌 줄이 한 번이라도 나온
   //    "이후에" import 줄이 다시 나오면 → "import가 흩어져 있음" 오류
   ```

### 8.2 시험 환경 규칙 (콘텐츠를 쓸 때 반드시 지킬 것)

이 앱은 "합격"이 목적이므로, 실행은 되지만 **실기 환경에서 통하지 않는 코드**를 정답으로
싣지 않는다. 아래는 실제로 잘못 실려 있어 수정된 항목들이다.

1. **작업형1·3의 답은 `print()`로 출력해야 채점된다.** 변수에 담기만 하면 0점이다.
   해당 항목에는 화면에 상시 경고 배너(`print-notice`)를 띄우고, 스니펫도 가급적
   `print(...)` 형태로 싣는다.
2. **`test.csv`에는 정답(y)이 없다.** 따라서 `y_test`라는 변수는 실기에 존재하지 않는다.
   성능을 측정하는 코드는 반드시 train을 다시 나눈 검증 세트(`X_val`, `y_val`)를 쓴다.
3. **train/test를 각각 `get_dummies` 하면 컬럼이 어긋난다.**
   `X_test = X_test.reindex(columns=X_train.columns, fill_value=0)`를 항상 세트로 붙이거나
   `pd.concat` 후 분리한다. 작업형2 실패 원인 1순위.
4. **`xgboost`는 시험 환경에 없을 수 있다.** 주력 모델은 sklearn 내장
   (`RandomForest*`, `GradientBoosting*`)으로 잡고, xgboost 항목은 "설치 확인 필요"로
   표시해 예비용으로만 둔다.
5. **pandas 버전 함정을 피한다.**
   - `fillna(method='ffill')` (X) → `.ffill()` / `.bfill()` (O) — 2.1 deprecated, 3.0 삭제
   - `df.corr()` (X, 문자열 컬럼 있으면 2.0부터 ValueError) → `df.corr(numeric_only=True)` (O)
   - `mean_squared_error(..., squared=False)` (X, 버전별 변경) → `** 0.5` (O)
6. **문제 지시문의 "소수 n째 자리까지"와 "n째 자리에서"는 다른 지시다.**
   "둘째 자리까지" = `round(x, 2)`, "둘째 자리에서 반올림" = `round(x, 1)`.
   지시문과 정답 코드가 어긋나면 안 된다.
7. **샘플 표는 통계적으로 말이 되어야 한다.** 예: 표본 n개에서 z-score 절댓값의 최댓값은
   `(n-1)/√n`이므로, 4행짜리 표에 "|Z|>3인 이상치를 제거하라"고 쓰면 성립하지 않는다.
   전체 행 수를 시나리오에 명시하고 표는 "일부 발췌"로 표기한다. `value_counts().idxmax()`
   유형은 표 데이터에 동점이 없도록 만든다.

## 9. 통계 정확성 체크리스트 (검증 완료된 항목들)

- scipy 함수 반환값 언패킹 순서 확인됨: `shapiro`→(stat,p), `pearsonr`/`spearmanr`→(corr,p),
  `chi2_contingency`→(chi2,p,dof,expected), `f_oneway`→(stat,p), `levene`/`bartlett`→(stat,p),
  `mannwhitneyu`/`wilcoxon`/`kruskal`→(stat,p), `chisquare`→(stat,p).
- `train_test_split`, `LabelEncoder`(fit_transform은 train에만, transform은 test에)로
  데이터 누수 방지 패턴 유지.
- RMSE는 `mean_squared_error(...) ** 0.5`로 계산(사이킷런 버전별 `squared=False` 인자
  변경/제거 이슈를 피하기 위한 의도적 선택).
- 로지스틱 회귀 오즈비: `np.exp(model.params)`, 신뢰구간: `np.exp(model.conf_int())`.
  `smf.logit(...).fit(disp=0)`으로 최적화 로그가 답안 출력에 섞이지 않게 한다.
- 표준편차 ddof 주의: `scipy.stats.zscore`는 ddof=0(모표준편차), `df.std()`는 ddof=1
  (표본표준편차). 공식 답안은 대개 `df.std()` 기준이므로 pandas 식을 기본으로 싣는다.
- 단측검정은 `alternative='greater'/'less'`. 빠뜨리면 양측이 되어 p값이 2배가 되고
  결론이 뒤집힐 수 있다.
- **patsy 수식 문자열의 컬럼명은 파이썬 코드로 `eval`된다.** 따라서 `pass`, `class`, `yield`,
  `if`, `return` 같은 예약어를 컬럼명으로 쓰면 실행 즉시 `SyntaxError`가 난다
  (`smf.logit('pass ~ gpa', ...)`가 실제로 이 이유로 깨져 있었다 → `passed`로 개명).
  피할 수 없으면 `Q("pass")`로 감싸거나 `df.rename`으로 먼저 바꾼다.
  범주형은 반드시 `C()`로 감싸고, 교호작용은 `+`가 아니라 `*`.
  콘텐츠를 추가할 때는 예약어 스캐너로 자동 검사할 것(§11).
- 변수를 덮어쓴 뒤 그 변수로 다시 값을 계산하지 않는다. 실제로 두 곳에서 발견되었다:
  `X = all_df[:len(X)]` 다음 줄의 `len(X)`(→ `n_train`을 먼저 저장),
  `X = X.fillna(X.median())` 다음 줄의 `X.median()`(→ `med`를 먼저 저장).
- `str.contains`에는 `na=False`를 붙인다. 결측치가 있으면 불리언 마스킹이 예외로 멈춘다.
- pandas 인덱스 정렬 주의: `pd.DataFrame({'id': test_id, 'pred': y_pred})`에서 `test_id`는
  인덱스를 가진 Series, `y_pred`는 배열이라 인덱스가 흐트러져 있으면 값이 어긋난다.

## 11. 콘텐츠 자동 검사 (새 문항을 추가할 때마다 돌릴 것)

DATA 배열을 파싱해 아래 셋을 기계적으로 확인한다. 사람 눈으로는 놓친다.

1. **import 규칙**: 별칭(`np.` `stats.` `smf.` `sm.`)이나 함수명을 쓰는데 같은 코드 문자열
   안에 해당 import가 없으면 오류. import 아닌 줄이 나온 뒤 다시 import가 나오면 오류.
2. **파이썬 예약어 충돌**: patsy 수식 문자열 안의 식별자와 샘플 표 컬럼명을 예약어 목록과
   대조(`Q("...")`로 감싼 것은 제외).
3. **동작 테스트**: jsdom으로 실제 DOM에서 렌더링·제출·피드백·저장·복원·단축키·정답 접기·
   검색·초기화를 확인. 총 문항 수는 하드코딩하지 말고 DATA에서 계산할 것.

## 10. 재구현 체크리스트 (요약)

1. 단일 HTML 파일, vanilla JS IIFE, 위 색상 변수로 라이트 테마 CSS 작성.
2. 헤더(진행률바+오답노트 토글+전체 초기화) → 탭 nav → 2열 workbench(사이드바+practice).
3. DATA 배열(§8 스키마)로 4개 탭 콘텐츠 작성, §8.1 import 규칙과 §8.2 시험 환경 규칙을
   반드시 준수.
4. 사이드바: 카테고리 접이식 목록 + 항목 상태 dot + 배지, "전체 함수 사전" 탭은 검색형
   평면 리스트. 사전 탭 행에도 `data-id`를 붙여 선택 하이라이트가 동작하게 할 것.
5. practice 패널: print 경고(작업형1·3) → 정보 패널 → 문제 설명(모의고사만) →
   입력 textarea(제출/다시시도) → 힌트 → 정답 코드(모의고사는 기본 접힘).
6. **채점은 오직 "제출" 액션(버튼 또는 Ctrl+Enter)에서만** 실행. 입력 중에는 정확도%/
   타이머만 갱신하고 정오 판정/렌더링은 절대 하지 않는다.
7. 제출 시 §5.3 정규화 후 일치하면 완료 처리, 아니면 입력한 문자열 기준 글자별 diff를
   결과 패널에 1회 렌더링 + 오답 영구 플래그.
8. 진행률/카테고리 배지/오답노트 필터 갱신은 "실제로 상태가 바뀌는 시점"에만 호출해서
   불필요한 반복 DOM 순회를 피한다.
9. 타이머는 `startTimer(id)` 한 곳에서만 켠다. 항목을 떠날 때 `deactivateCurrent()`가
   `running=false`로 만들므로, **다시 들어올 때 `st.typed`가 비어 있지 않으면 반드시
   타이머를 재개**해야 한다. (예전에는 `if(st.running)`으로만 판단해 한 번 떠난 항목의
   시간이 영구히 멈추는 버그가 있었다.)
10. 상태 변경 지점마다 `saveState()`(디바운스) 또는 `saveStateNow()`를 호출한다(§6.1).
